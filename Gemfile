source "https://rubygems.org"

# Mirrors the GitHub Pages build environment, so `bundle exec jekyll serve`
# locally behaves like the deployed site.
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"

group :jekyll_plugins do
  gem "jekyll-sitemap"
end
